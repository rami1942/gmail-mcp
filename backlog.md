# backlog.md - タスク一覧

---
## T0001 ラベル指定でのフィルター取得・検索機能(get_filters_by_label)
- **状態** : 未着手
- **要求** : 
get_filters(label_name="test/filter1")のように、ラベル名(可読)を指定して関連するフィルターのみを取得できる機能の実装

---
## T0002 フィルターの重複・競合検出機能(analyze_filter_overlaps)
- **状態** : 未着手
- **要求** : 
複数の指定ラベル間または全フィルターを対象に、送信元アドレス（from）やクエリ（query）の完全一致・ドメイン重複・同一フィルター内の多重登録などを自動解析し、重複・競合レポートを出力する機能の実装

---
## T0003 フィルターの作成・更新・削除機能(create_filter / update_filter / delete_filter)
- **状態** : 完了(分割)
- **要求** : 
重複条件の除去やフィルターの統合・新規追加を対話経由で完結できるよう、フィルターの作成・更新・削除（CRUD）を行えるMCPツールの実装
- **備考** : T0007, T0008に分割し、それぞれで処理する
---
## T0004 search_emails の返却情報拡充（snippet, labels, headers）
- **状態** : 未着手
- **要求** : 
search_emails の返却データに以下を追加し、一覧取得時点でメールの分類やマーケティングメール判定を行えるようにする
  - `snippet`: 本文冒頭テキスト（約100文字）
  - `labels`: 付与されている全ラベル（CATEGORY_PROMOTIONS, UNREAD等のシステムラベル含む）
  - `has_unsubscribe`: List-Unsubscribe ヘッダーの有無などの判定フラグ

---
## T0005 メールの一括取得機能(batch_read_emails)
- **状態** : 未着手
- **要求** : 
複数のメッセージIDを配列で指定し、各メールの本文・ヘッダー情報を一度のAPIリクエスト（または一括処理）で取得できるツールの実装（個別 read_email の呼び出し回数と待ち時間の削減）

---
## T0006 MCPサーバー標準出力（stdio）保護とログ出力の stderr 統一
- **状態** : 完了
- **要求** : 
stdio トランスポートでの JSON-RPC 通信破損を防ぐため、サーバー起動ログやデバッグ用 print 出力をすべて `sys.stderr` へ出力するようコード全体のログ方式を徹底・統一する

---

## T0007 フィルターの作成・削除機能(create_filter / delete_filter)
- **状態** : 完了
- **要求** : 
重複条件の除去やフィルターの統合・新規追加を対話経由で完結できるよう、フィルターの作成・削除を行えるMCPツールの実装
Gmail APIのリファレンスは以下を参照のこと。
https://developers.google.com/workspace/gmail/api/reference/rest?hl=ja


---
## T0003 フィルターの更新機能(update_filter)
- **状態** : 未着手
- **要求** : 
重複条件の除去やフィルターの統合・新規追加を対話経由で完結できるよう、フィルターの更新を行えるMCPツールの実装
- **備考** :
Gmail APIにはフィルターの更新は提供されていないためタスクを分割して個別に対処する
